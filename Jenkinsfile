pipeline{
    agent any
    stages{
        stage("Git checkout")
            when {
                branch 'master'
        }
            steps{
                echo "Git Checkout Completed"
        }
    }
}
